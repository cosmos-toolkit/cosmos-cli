# {{.ProjectName}}

A Go HTTP API service.

## Getting Started

```bash
go run cmd/api/main.go
```

The server will start on `:8080`.

## Project Structure

- `cmd/api/` - Application entry point
- `internal/server/` - HTTP server setup
- `internal/handler/` - HTTP handlers
